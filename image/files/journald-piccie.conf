[Journal]
# Read-only root: keep the journal in RAM (volatile) so it never writes to the
# (read-only) root fs. /var/log is also a tmpfs (see fstab).
Storage=volatile
RuntimeMaxUse=48M
ForwardToSyslog=no
Compress=yes
